{
  "name": "google-calendar-event-creator",
  "version": "1.0.0",
  "description": "Chrome extension that creates a pre-filled Google Calendar event from the event details on the current web page.",
  "private": true,
  "scripts": {
    "test": "node --test \"test/**/*.test.js\"",
    "test:offline": "node --test test/extraction.test.js test/calendar-url.test.js",
    "test:live": "node --test test/live.test.js",
    "refresh": "node test/refresh-snapshots.js"
  },
  "license": "MIT",
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
