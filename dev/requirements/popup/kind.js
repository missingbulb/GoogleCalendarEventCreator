// Kind: popup — a popup requirement leaf, verified by the popup's REAL render()
// (satori) producing a PNG that is compared pixel-exact to the committed snapshot
// beside the case (cases/<slug>.<id>.png). The default, most common kind.
// Runner + comparison: shared/render/visual-snapshots.test.js (shared with icon).
"use strict";

module.exports = { image: true };
