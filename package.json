{
  "name": "google-calendar-event-creator",
  "version": "1.2.0",
  "description": "Chrome extension that creates a pre-filled Google Calendar event from the event details on the current web page.",
  "private": true,
  "scripts": {
    "test": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test \"test/unit/**/*.test.js\" \"executable-requirements/extractors/**/*.test.js\" \"test/extension/**/*.test.js\" \"test/uber/**/*.test.js\" \"executable-requirements/ui/**/*.test.js\" \"executable-requirements/ui-requirements-coverage.test.js\" \"executable-requirements/product-requirements.test.js\"",
    "test:offline": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test test/unit/extraction.test.js test/unit/calendar-url.test.js test/unit/source-request.test.js test/unit/shipping-files.test.js test/unit/load-order-generated.test.js test/unit/load-order.test.js test/unit/popup-content.test.js test/unit/popup-truncation.test.js test/unit/events-view.test.js test/unit/triage-extractor-request.test.js test/unit/attach-sample-url.test.js test/unit/extractor-naming.test.js test/unit/extractor-scaffold.test.js test/unit/probe-challenge.test.js test/unit/case-quality.test.js test/unit/supported-domains.test.js test/unit/spa-shell.test.js test/unit/docs-reachable.test.js test/extension/toolbar-icon-state.test.js test/extension/toolbar-icon-paths.test.js test/extension/extension-loads.test.js test/uber/shared-constants.test.js executable-requirements/product-requirements.test.js",
    "test:live": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test executable-requirements/extractors/live.test.js executable-requirements/extractors/extractor-support.test.js executable-requirements/extractors/fallback/fallback-coverage.test.js",
    "test:ui": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test executable-requirements/ui/popup-snapshots.test.js",
    "test:e2e": "node --test \"executable-requirements/fullBrowserHeavyTests/**/*.test.js\"",
    "build": "node tools/build-zip.js",
    "index": "node tools/index.js",
    "refresh": "node data/refresh-cache.js",
    "refresh:ui": "node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON executable-requirements/infrastructure/refresh-popup-snapshots.js",
    "regen": "node tools/index.js && node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON executable-requirements/infrastructure/refresh-popup-snapshots.js && node --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test executable-requirements/extractors/fallback/fallback-coverage.test.js"
  },
  "license": "MIT",
  "_scriptNotes": {
    "--disable-warning=MODULE_TYPELESS_PACKAGE_JSON": "Deliberate, reviewed suppression (issue #305). The popup's ES-module graph (extension/config.js, extension/fallback-policy.js, extension/pipeline/build-calendar-url.js, extension/ui/popup.js, extension/ui/views/*.js) is loaded as .js with no package.json \"type\" (the rest of the repo is CommonJS), so Node parses-as-CJS-then-reparses-as-ESM and warns on every test run. This is a Node-loader-only artifact: the shipped extension loads these via <script type=\"module\"> in extension/ui/popup.html and Chrome never reads package.json, so nothing is wrong in production. Suppressing only this one code keeps other warnings visible (see the warnings rule in docs/claude/workflow.md). Applied only to the node invocations that import the ESM graph."
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "jsdom": "^29.1.1",
    "pixelmatch": "^7.2.0",
    "pngjs": "^7.0.0",
    "satori": "^0.26.0"
  },
  "_dependencyNotes": {
    "jsdom": "Test-only. Provides a DOM implementation (test/harness.js) so the unmodified extractor scripts (extension/pipeline/) can be run and asserted against in Node, exactly as they execute in a browser. Not shipped with the extension.",
    "satori": "Test-only. Renders the popup's HTML/CSS-subset tree to SVG for UI snapshot tests (executable-requirements/infrastructure/popup-renderer.js), since it isn't a real screenshot of the popup.",
    "@resvg/resvg-js": "Test-only. Rasterizes the SVG produced by satori into a PNG for UI snapshot tests (executable-requirements/infrastructure/popup-renderer.js).",
    "pngjs": "Test-only. Decodes/encodes the PNG snapshots compared in the UI tests (executable-requirements/ui/popup-snapshots.test.js).",
    "pixelmatch": "Test-only. Performs pixel-level diffing between rendered and reference PNGs in the UI tests (executable-requirements/ui/popup-snapshots.test.js)."
  }
}
