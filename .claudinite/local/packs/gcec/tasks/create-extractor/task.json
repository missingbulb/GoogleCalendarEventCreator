{
  "$schema": "../../../../../shared/packs/claudinite-tasks/task.schema.json",
  "id": "create-extractor",
  "description": "Turns an extractor-request issue into a PR adding site support: preprocessing triages the request, fetches and records the target page, and scaffolds the extractor; the agent then writes extract() against that recorded page and fills in the test case.",
  "trigger": "schedule",
  "preconditions": [
    "schedule:at-most-daily",
    "extractor-request-eligible"
  ],
  "expected_outcome": "fresh_pr",
  "automerge": [
    "site-support-scope"
  ],
  "agent_model": "sonnet",
  "agent_instructions": "task.md",
  "agent_execution_timeout": 3600,
  "code_work": "node prepare.mjs",
  "code_work_timeout": 1800,
  "code_work_required_secrets": [
    "SCRAPER_API_KEY"
  ]
}
