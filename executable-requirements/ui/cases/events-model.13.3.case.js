// Logic leaf 13.3 (tracked, untested in the executable runner): An event's instances are grouped BY MONTH into one or more cards — a single card for a month with one showing, or a grouped card with a button per showing.
// Currently covered by executable-requirements/ui/cases/event-cards-grouping.4.2.1.case.js. Marked tbd until a faithful executable
// validation is wired here (see docs/claude/testing.md on the kind:"logic" path).
"use strict";

module.exports = {
  kind: "logic",
  tbd: true,
  description: "An event's instances are grouped BY MONTH into one or more cards — a single card for a month with one showing, or a grouped card with a button per showing.",
  coveredBy: "executable-requirements/ui/cases/event-cards-grouping.4.2.1.case.js",
};
