// Kind: logic — a non-visual product/behavior rule (§12–§16): popup states, the
// events model, field rules, timezone rules, the support-request flow. A wired case
// carries an executable verify() asserting the rule against the real shipped code;
// a tbd case names where it is covered today (coveredBy). The owner-approved
// expected is the coded assertion. Runner: logic/product-requirements.test.js.
"use strict";

module.exports = { image: false };
