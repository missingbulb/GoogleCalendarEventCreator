// Kind: behavior — a click/navigation a static image cannot observe (3.4, 9.1–9.3):
// clicking a card / instance button / affordance link opens the right URL in an
// adjacent new tab and closes the popup. No PNG; the owner-approved expected is the
// coded assertion in the runner. Runner: behavior/events-view-actions.test.js.
"use strict";

module.exports = { image: false };
